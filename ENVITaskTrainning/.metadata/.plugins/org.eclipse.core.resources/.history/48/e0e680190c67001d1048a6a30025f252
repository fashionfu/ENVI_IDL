PRO test_VFCTask_UI

  COMPILE_OPT IDL2
  e = ENVI(/CURRENT)
  
  ;当自定义 ENVITask 未部署到 ENVI 路径时，可以使用 task 文件完整路径进行初始
  taskfile = FILEPATH('VFCTask.task', root_dir=ROUTINE_DIR())
  Task = ENVITask(taskfile)
  
  
END